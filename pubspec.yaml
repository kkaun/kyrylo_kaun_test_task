name: kyrylo_kaun_test_task
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  # GraphQL client
  graphql_flutter: ^5.0.0
  # Standard BLoC implementation solution for Flutter
  flutter_bloc: ^7.3.0
  # Functional data processing utils
  dartz: ^0.10.0
  # Reduce objects comparability boilerplate
  equatable: ^2.0.3 
  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  # Unit testing with mock dependencies
  mockito: ^5.0.16
  # BLoC testing
  bloc_test: ^8.2.0
  # Code generation (only for tests here)
  build_runner:

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^1.0.0

# The following section is specific to Flutter.
flutter:

  uses-material-design: true